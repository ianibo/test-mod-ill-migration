micronaut.application.name=mod-ill-mn

# Datasource Configuration
datasources.default.url=${JDBC_URL:jdbc:postgresql://localhost:5432/postgres}
datasources.default.driver-class-name=org.postgresql.Driver
datasources.default.username=${JDBC_USER:postgres}
datasources.default.password=${JDBC_PASSWORD:secret}
datasources.default.db-type=postgres
datasources.default.dialect=POSTGRES

# Flyway Configuration
flyway.datasources.default.enabled=true
flyway.datasources.default.locations=classpath:db/migration

# Swagger UI
micronaut.router.static-resources.swagger.paths=classpath:META-INF/swagger
micronaut.router.static-resources.swagger.mapping=/swagger/**
micronaut.router.static-resources.swagger-ui.paths=classpath:META-INF/swagger/views/swagger-ui
micronaut.router.static-resources.swagger-ui.mapping=/swagger-ui/**